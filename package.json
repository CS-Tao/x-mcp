{
  "name": "ext-mcp",
  "version": "1.0.2",
  "description": "Extendable MCP Framework",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "import": "./es/index.js",
      "require": "./lib/index.js"
    }
  },
  "files": [
    "lib",
    "es"
  ],
  "scripts": {
    "clean": "rimraf lib es",
    "build": "npm run clean && tsc -p tsconfig.build.json && tsc -p tsconfig.build.es.json",
    "demo": "npm run build && tsx demo",
    "test": "tsc -p tsconfig.demo.json && mcp-inspector --cli npm run demo --method tools/call --tool-name say-hello --tool-arg name=ext-mcp | grep -q 'Hello, ext-mcp!' || (echo 'test failed' && exit 1)",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.17.2",
    "@types/node": "24.10.0",
    "typescript": "^5.9.3",
    "rimraf": "^6.1.0",
    "tsx": "^4.20.6"
  },
  "engines": {
    "node": ">= 18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CS-Tao/x-mcp"
  },
  "bugs": {
    "url": "https://github.com/cs-tao/x-mcp/issues"
  },
  "homepage": "https://github.com/cs-tao/x-mcp#readme",
  "keywords": ["mcp", "modelcontextprotocol", "x-mcp", "ext-mcp"]
}